#ifndef LOGGER_H_INCLUDED
#define LOGGER_H_INCLUDED

#include <string>
#include <map>
#include <vector>
#include <tuple>

void initializeLogger(), print(std::string), debug(std::string), archive(std::string);

#endif

